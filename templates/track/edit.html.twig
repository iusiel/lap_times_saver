{% extends 'admin.layout.html.twig' %}

{% block title %}Edit Track id:{{ track.id }}{% endblock %}

{% block content %}
    <div class="container">
        <h1>Edit Track id:{{ track.id }}</h1>

        {{ include('track/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('track/_delete_form.html.twig') }}

        <a href="{{ path('app_track_index') }}"><button class="btn btn-primary">Back to list</button></a>
    </div>
{% endblock %}
