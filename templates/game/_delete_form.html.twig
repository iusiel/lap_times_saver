<form class="mb-3 form__delete" method="post" action="{{ path('app_game_delete', {'id': game.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
