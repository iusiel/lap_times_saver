{% extends 'admin.layout.html.twig' %}

{% block title %}Edit Game id: {{ game.id }} {% endblock %}

{% block content %}
    <div class="container">
        <h1>Edit Game id: {{ game.id }}</h1>

        {{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('game/_delete_form.html.twig') }}

        <a href="{{ path('app_game_index') }}"><button class="btn btn-primary">Back to list</button></a>

        </div>
{% endblock %}
