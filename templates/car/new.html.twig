{% extends 'admin.layout.html.twig' %}

{% block title %}New Car{% endblock %}

{% block content %}
    <div class="container">
        <h1>Create new Car</h1>

        {{ include('car/_form.html.twig') }}

        {% if app.request.get('iframe') is empty %}
        <a href="{{ path('app_car_index') }}"><button class="btn btn-primary">Back to list</button></a>
        {% endif %}
    </div>
{% endblock %}
