<form class="mb-3 form__delete" method="post" action="{{ path('app_car_delete', {'id': car.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
