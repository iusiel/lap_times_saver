{% extends 'admin.layout.html.twig' %}

{% block title %}Edit Car id:{{ car.id }}{% endblock %}

{% block content %}
    <div class="container">
        <h1>Edit Car id:{{ car.id }}</h1>

        {{ include('car/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('car/_delete_form.html.twig') }}

        <a href="{{ path('app_car_index') }}"><button class="btn btn-primary">Back to list</button></a>

    </div>
{% endblock %}
